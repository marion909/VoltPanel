<?php
/**
 * Von VoltPanel erzeugt — nicht von Hand bearbeiten.
 * Jede Änderung geht beim nächsten Schreiben verloren.
 *
 * Was diese Datei nicht enthält: Anpassungen der eigenen Installation. Die
 * gehören in wp-config.php erst *nach* dieser Zeile — dieselbe Grenze, die
 * WordPress selbst zieht ("That's all, stop editing!").
 */

define( 'DB_NAME', '{{ .DBName | phpstr }}' );
define( 'DB_USER', '{{ .DBUser | phpstr }}' );
define( 'DB_PASSWORD', '{{ .DBPassword | phpstr }}' );
define( 'DB_HOST', 'localhost' );
define( 'DB_CHARSET', 'utf8mb4' );
define( 'DB_COLLATE', '' );

define( 'AUTH_KEY', '{{ .AuthKey | phpstr }}' );
define( 'SECURE_AUTH_KEY', '{{ .SecureAuthKey | phpstr }}' );
define( 'LOGGED_IN_KEY', '{{ .LoggedInKey | phpstr }}' );
define( 'NONCE_KEY', '{{ .NonceKey | phpstr }}' );
define( 'AUTH_SALT', '{{ .AuthSalt | phpstr }}' );
define( 'SECURE_AUTH_SALT', '{{ .SecureAuthSalt | phpstr }}' );
define( 'LOGGED_IN_SALT', '{{ .LoggedInSalt | phpstr }}' );
define( 'NONCE_SALT', '{{ .NonceSalt | phpstr }}' );

$table_prefix = '{{ .TablePrefix | phpstr }}';

define( 'WP_DEBUG', false );

if ( ! defined( 'ABSPATH' ) ) {
	define( 'ABSPATH', __DIR__ . '/' );
}
require_once ABSPATH . 'wp-settings.php';
